{
  "name": "tw",
  "scripts": {
    "build": "rm -rf dist && parcel build src/index.html --public-url . --no-cache",
    "dev": "parcel serve src/index.html"
  },
  "dependencies": {
    "parcel-bundler": "^1.12",
    "posthtml-include": "^1.6",
    "tailwindcss": "^1.9"
  },
  "browserslist": [
    "defaults"
  ],
  "devDependencies": {
    "tailwindcss-debug-screens": "^1.1"
  }
}
