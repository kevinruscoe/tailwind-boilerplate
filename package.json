{
  "name": "tw",
  "scripts": {
    "build": "rm -rf dist && parcel build src/index.html",
    "dev": "parcel serve src/index.html"
  },
  "dependencies": {
    "parcel-bundler": "^1.12.4",
    "tailwindcss": "^1.6.1"
  },
  "browserslist": [
    "defaults"
  ]
}
