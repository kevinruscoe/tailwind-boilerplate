{
  "name": "tw",
  "scripts": {
    "build": "rm -rf dist && parcel build src/index.html --public-url . --no-cache",
    "dev": "parcel serve src/index.html"
  },
  "dependencies": {
    "autoprefixer": "^9.8.6",
    "parcel-bundler": "^1.12.4",
    "postcss": "^7.0.35",
    "posthtml-include": "^1.6.0",
    "tailwindcss": "^2.0.1-compat"
  },
  "browserslist": [
    "defaults"
  ],
  "devDependencies": {
    "tailwindcss-debug-screens": "^1.1.0"
  }
}
